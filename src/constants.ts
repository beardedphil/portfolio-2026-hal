/** localStorage prefix for conversation persistence (fallback when no project DB). */
export const CONVERSATION_STORAGE_PREFIX = 'hal-chat-conversations-'
/** Cap on character count for recent conversation (~3k tokens). */
export const CONVERSATION_RECENT_MAX_CHARS = 12_000

export const THEME_STORAGE_KEY = 'hal-theme'
