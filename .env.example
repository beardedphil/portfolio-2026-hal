# Copy to .env and fill in. Local-only; not committed.
# Frontend (Vite): used for auto-connect to Supabase on load.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...
# Backend/scripts (e.g. npm run sync-tickets)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=eyJ...
# Optional: SERVICE_ROLE_KEY bypasses RLS; needed for agent_artifacts insert from /api/agent-runs/status.
# SUPABASE_SERVICE_ROLE_KEY=eyJ...
# Optional: GitHub PAT with repo read access; used to fetch implementation artifacts from PR branch when session has no token.
# GITHUB_TOKEN=ghp_...
# Direct REST calls to Supabase need the anon key: header "apikey: <SUPABASE_ANON_KEY>" or query ?apikey=<key>
# HAL chat Project Manager (server-only; do not prefix with VITE_)
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
# Cursor API (for Implementation and QA Cloud Agents)
# CURSOR_API_KEY: server-only, used by /api/implementation-agent/run, /api/qa-agent/run, /api/agent-runs/launch (do not prefix with VITE_)
# VITE_CURSOR_API_KEY: exposed to frontend for "Configured" status display only; set same value as CURSOR_API_KEY
CURSOR_API_KEY=your-cursor-api-key
VITE_CURSOR_API_KEY=your-cursor-api-key
# Optional: model for Cursor Cloud Agents (e.g. claude-4-sonnet, gpt-5.2). Omit for auto-selection.
# CURSOR_AGENT_MODEL=claude-4-sonnet
# CURSOR_IMPLEMENTATION_MODEL=claude-4-sonnet
# CURSOR_QA_MODEL=gpt-5.2

# GitHub OAuth (0079)
# Create a GitHub OAuth App and set callback URL to:
#   https://<your-domain>/api/auth/github/callback
GITHUB_CLIENT_ID=github-oauth-client-id
GITHUB_CLIENT_SECRET=github-oauth-client-secret
# Used to encrypt the httpOnly session cookie (must be long/random, 32+ chars).
# Required on Vercel for /api/auth/* (Sign into GitHub). Set in Project → Settings → Environment Variables.
AUTH_SESSION_SECRET=replace-with-long-random-string
# On Vercel: set to your deployment URL so OAuth redirects work (e.g. https://portfolio-2026-hal.vercel.app).
APP_ORIGIN=https://your-domain.example
# Encryption key for secrets at rest (OAuth tokens, Supabase service keys).
# Must be 32 bytes (64 hex chars) or longer. If shorter, will be hashed to 32 bytes.
# Required for secret encryption. Set in Project → Settings → Environment Variables.
HAL_ENCRYPTION_KEY=replace-with-64-hex-char-key-or-longer-random-string